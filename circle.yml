test:
  override:
    - npm run lint
    - npm test
  post:
    - mv spec/junitresults "$CIRCLE_TEST_REPORTS/junitresults"
deployment:
  release:
    branch: master
    commands:
      - make deploy
  staging:
    branch: dev
    commands:
      - git fetch --unshallow
      - make deploy-dev
